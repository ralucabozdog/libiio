/*
 * Copyright (c) 2025 Analog Devices, Inc.
 *
 * SPDX-License-Identifier: MIT
 */

/ {
	iio-context {
		#address-cells = <1>;
		#size-cells = <0>;

		iio-device@0 {
			compatible = "iio,io-channels";
			reg = <0>;
			io-channels = <&adc 0>;
			io-channel-names = "voltage0";
			buffer-name = "buffer0";
			trigger-device = <&trigger0>;
		};

		trigger0: iio-device@1 {
			compatible = "iio,trigger-timer";
			reg = <1>;
			trigger-id = "trigger0";
			trigger-type = "timer";
		};
	};
};

&adc_clk_ext_p0_9 {
	power-source = <MAX32_VSEL_VDDIOH>;
};

&adc {
	/* Override compatible to enable streaming/FIFO support */
	compatible = "adi,max32-adc-b-me18", "adi,max32-adc";
	pinctrl-0 = <&adc_clk_ext_p0_9>;
	pinctrl-names = "default";

	#address-cells = <1>;
	#size-cells = <0>;
	#io-channel-cells = <1>;
	status = "okay";

	channel@0 {
		reg = <0>;
		zephyr,gain = "ADC_GAIN_1";
		zephyr,reference = "ADC_REF_INTERNAL";
		zephyr,acquisition-time = <ADC_ACQ_TIME_DEFAULT>;
		zephyr,resolution = <12>;
		zephyr,vref-mv = <1250>;
	};
};

